function area() {
    let radius = document.getElementById("radiusval").value;
    let result = Math.PI * radius * radius;
    document.getElementById("areaval").value = result.toFixed(2);
    onclick="radius"
    document="A.tofixed(2)";
}